//various utility functions that are needed during treatment derivation.

//calculate the average between 2 numbers, to the nearest int.
avgOfTwo(int a, int b) {
  return (a + b) / 2 as int;
}

avgOfFour(int a, int b, int c, int d) {
  return (a + b + c + d) / 4 as int;
}
